<% if user_signed_in? %>
<div class="action-sheet">


  <div class="row">
    <div class="col-xs-6">
      <% if (params[:controller] == "profile" && params[:action] == "show") || params[:controller] == "subordinates/expenses"%>
        <%= link_to "Reporting", subordinates_expenses_path, class: "btn btn-default" %>
      <% end %>
    </div>
    <div class="col-xs-6">
      <%= link_to 'Add', new_expense_path, class: "btn btn-success"%>
    </div>
  </div>
</div>
<% end %>
